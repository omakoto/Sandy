# Sandy

- **Main script**: scripts/sandy.sh

This script launches Bash in a sandbox where almost all the filesystem is visible as-is, except $HOME/.ssh/ and $HOME/.config/ do not exist.

## Detailed requirements:

